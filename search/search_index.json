{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"Project layout","text":"<pre><code>mkdocs.yml    # The configuration file.\ndocs/\n    index.md  # The documentation homepage.\n    ...       # Other markdown pages, images and other files.\n</code></pre>"},{"location":"reference/miniaturize/","title":"Miniaturization Module","text":"<p>An MH-based annealer to miniaturize a graph</p> Attritubes <ul> <li> <p>metrics_dict: A dictionary of function handles used by the annealer to   calculate the functio metrics</p> </li> <li> <p>beta: The initial inverse temperature of the replica</p> </li> <li> <p>graph_: The generated miniature</p> </li> </ul> <p>Corsens an unweighted graph</p> Source code in <code>src/minigraphs/miniaturize.py</code> <pre><code>class CoarseNET:\n    '''Corsens an unweighted graph\n\n    '''\n\n    def __init__(self,alpha,G):\n        '''Initializes coarsener \n        '''\n        self.alpha = alpha\n        self.G = deepcopy(G)\n\n    @property\n    def alpha(self):\n        '''Shrinkage factor'''\n        return self._alpha\n\n    @alpha.setter\n    def alpha(self,val):\n        #TODO: Validate within range (0,1.0)\n        self._alpha = val\n\n    @property\n    def G(self):\n        '''Original Graph\n        '''\n        return self._G\n\n    @G.setter\n    def G(self,Graph):\n        #TODO: Validate strongly connected graph\n        self._G = Graph\n\n    @staticmethod\n    def adjacency(G):\n        '''Computes the adjacency matrix of a Graph\n        '''\n        A = nx.to_scipy_sparse_array(G)\n        A = A._asfptype()\n        A = normalize(A,norm='l2',axis=0)\n\n        return A\n\n    @staticmethod\n    def eigs(G):\n        '''Computes the dominant eigenvalue and eigenvectors of the Adjacency matrix of a graph\n        '''\n        # Adjacency Matrix\n        A = CoarseNET.adjacency(G)\n\n        # Compute the first eigenvalue and right eigenvector\n        lambda_, u_ = scipy.sparse.linalg.eigs(A,k=1)\n\n        # Compute the left eigenvector\n        _, v_= scipy.sparse.linalg.eigs(A.T,k=1)\n\n        return np.real(lambda_)[0], np.real(np.squeeze(u_)), np.real(np.squeeze(v_))\n\n    def __edge_score(self,edge):\n        '''Calculates the score of a node pair\n        '''\n        u_a, u_b = self.u_[edge[0]], self.u_[edge[1]]\n        v_a, v_b = self.v_[edge[0]], self.v_[edge[1]]\n\n        prod = (self.lambda_-1)*(u_a+u_b)\n        score = (-self.lambda_*(u_a*v_a+u_b*v_b) + v_a*prod + u_a*v_b + u_b*v_a) / (np.dot(self.v_,self.u_)-(u_a*v_a + u_b*v_b))  \n\n        return score\n\n    def __score(self):\n        '''Calculates the score for all the edges in the graph\n        '''\n        # Initialize array of scores\n        score = np.zeros(self.G_coarse_.number_of_edges())\n\n        # Calculate score for every edge in the graph\n        for i, edge in enumerate(self.G_coarse_.edges):\n            score[i] = self.__edge_score(edge)\n\n        return np.abs(score)\n\n    def __contract(self,edge) -&gt; bool:\n        '''Updates graph by contracting nodes in the edge\n        '''\n        # Upack nodes\n        u, v = edge\n        left, right = self.nodes_coarse_[u], self.nodes_coarse_[v]\n\n        contract = left != right\n        if contract:\n            # Merge nodes\n            nx.contracted_nodes(self.G_coarse_,\n                                left,\n                                right,\n                                self_loops=False,\n                                copy=False)\n\n            # Update node index in coarsened graph\n            idx = self.nodes_coarse_ == right\n            self.nodes_coarse_[idx] = left \n            self.nodes_removed_.append(right)\n\n        return contract\n\n    def coarsen(self) -&gt; None:\n        '''Coarsens the graph\n        '''\n        self.G_coarse_ = self._G.to_directed()\n        n = self.G_coarse_.number_of_nodes()\n        n_edges = self.G_coarse_.number_of_edges()\n        n_reduced = int(self._alpha * n)\n\n        # Compute the eigenvalue and eigenvectors\n        self.lambda_, self.u_, self.v_ = CoarseNET.eigs(self.G_coarse_)\n\n        # Arrays of nodes and edges\n        self.nodes_coarse_ = np.arange(0,n,dtype=np.int32)\n        self.nodes_removed_ = []\n        edges = list(self.G_coarse_.edges)\n\n        # Calculate sorting indices according to score\n        score = self.__score()\n        idx = np.argsort(score)\n\n        contractions = 0\n        i = 0\n        while (contractions &lt; n_reduced) and (i &lt; n_edges):\n            # Retrieve edge according to sorting\n            edge = edges[idx[i]]\n\n            # Contract edges\n            contract = self.__contract(edge)\n\n            if contract:\n                contractions += 1\n\n            i += 1\n</code></pre>"},{"location":"reference/miniaturize/#minigraphs.miniaturize.MH.__accept_change","title":"<code>__accept_change(E0, E1)</code>","text":"<p>Accepts proposed change according to the Metropolis ratio</p>"},{"location":"reference/miniaturize/#minigraphs.miniaturize.MH.__energy","title":"<code>__energy(metrics)</code>","text":"<p>Energy of the graph with respect to target metrics</p>"},{"location":"reference/miniaturize/#minigraphs.miniaturize.MH.__get_metrics","title":"<code>__get_metrics()</code>","text":"<p>Calculates the metrics of a graph</p>"},{"location":"reference/miniaturize/#minigraphs.miniaturize.MH.__init__","title":"<code>__init__(metrics, schedule=None, weights=None, n_changes=1, func_loss=None)</code>","text":"<p>Instantiates the MH annealer</p>"},{"location":"reference/miniaturize/#minigraphs.miniaturize.MH.__make_change","title":"<code>__make_change()</code>","text":"<p>Implements changes in a graph</p>"},{"location":"reference/miniaturize/#minigraphs.miniaturize.MH.__schedule_adaptive","title":"<code>__schedule_adaptive(step)</code>","text":"<p>Provides an adaptive scheduling</p>"},{"location":"reference/miniaturize/#minigraphs.miniaturize.MH.__stop_convergence","title":"<code>__stop_convergence(step)</code>","text":"<p>Provide a convergence criterion based on the proximity to the metrics</p>"},{"location":"reference/miniaturize/#minigraphs.miniaturize.MH.transform","title":"<code>transform(graph_seed, targets, n_iterations=None, epsilon=None, beta=None, verbose=False)</code>","text":"<p>Miniaturizes seed graph</p>"},{"location":"reference/miniaturize/#minigraphs.miniaturize.CoarseNET.G","title":"<code>G</code>  <code>property</code> <code>writable</code>","text":"<p>Original Graph</p>"},{"location":"reference/miniaturize/#minigraphs.miniaturize.CoarseNET.alpha","title":"<code>alpha</code>  <code>property</code> <code>writable</code>","text":"<p>Shrinkage factor</p>"},{"location":"reference/miniaturize/#minigraphs.miniaturize.CoarseNET.__contract","title":"<code>__contract(edge)</code>","text":"<p>Updates graph by contracting nodes in the edge</p> Source code in <code>src/minigraphs/miniaturize.py</code> <pre><code>def __contract(self,edge) -&gt; bool:\n    '''Updates graph by contracting nodes in the edge\n    '''\n    # Upack nodes\n    u, v = edge\n    left, right = self.nodes_coarse_[u], self.nodes_coarse_[v]\n\n    contract = left != right\n    if contract:\n        # Merge nodes\n        nx.contracted_nodes(self.G_coarse_,\n                            left,\n                            right,\n                            self_loops=False,\n                            copy=False)\n\n        # Update node index in coarsened graph\n        idx = self.nodes_coarse_ == right\n        self.nodes_coarse_[idx] = left \n        self.nodes_removed_.append(right)\n\n    return contract\n</code></pre>"},{"location":"reference/miniaturize/#minigraphs.miniaturize.CoarseNET.__edge_score","title":"<code>__edge_score(edge)</code>","text":"<p>Calculates the score of a node pair</p> Source code in <code>src/minigraphs/miniaturize.py</code> <pre><code>def __edge_score(self,edge):\n    '''Calculates the score of a node pair\n    '''\n    u_a, u_b = self.u_[edge[0]], self.u_[edge[1]]\n    v_a, v_b = self.v_[edge[0]], self.v_[edge[1]]\n\n    prod = (self.lambda_-1)*(u_a+u_b)\n    score = (-self.lambda_*(u_a*v_a+u_b*v_b) + v_a*prod + u_a*v_b + u_b*v_a) / (np.dot(self.v_,self.u_)-(u_a*v_a + u_b*v_b))  \n\n    return score\n</code></pre>"},{"location":"reference/miniaturize/#minigraphs.miniaturize.CoarseNET.__init__","title":"<code>__init__(alpha, G)</code>","text":"<p>Initializes coarsener</p> Source code in <code>src/minigraphs/miniaturize.py</code> <pre><code>def __init__(self,alpha,G):\n    '''Initializes coarsener \n    '''\n    self.alpha = alpha\n    self.G = deepcopy(G)\n</code></pre>"},{"location":"reference/miniaturize/#minigraphs.miniaturize.CoarseNET.__score","title":"<code>__score()</code>","text":"<p>Calculates the score for all the edges in the graph</p> Source code in <code>src/minigraphs/miniaturize.py</code> <pre><code>def __score(self):\n    '''Calculates the score for all the edges in the graph\n    '''\n    # Initialize array of scores\n    score = np.zeros(self.G_coarse_.number_of_edges())\n\n    # Calculate score for every edge in the graph\n    for i, edge in enumerate(self.G_coarse_.edges):\n        score[i] = self.__edge_score(edge)\n\n    return np.abs(score)\n</code></pre>"},{"location":"reference/miniaturize/#minigraphs.miniaturize.CoarseNET.adjacency","title":"<code>adjacency(G)</code>  <code>staticmethod</code>","text":"<p>Computes the adjacency matrix of a Graph</p> Source code in <code>src/minigraphs/miniaturize.py</code> <pre><code>@staticmethod\ndef adjacency(G):\n    '''Computes the adjacency matrix of a Graph\n    '''\n    A = nx.to_scipy_sparse_array(G)\n    A = A._asfptype()\n    A = normalize(A,norm='l2',axis=0)\n\n    return A\n</code></pre>"},{"location":"reference/miniaturize/#minigraphs.miniaturize.CoarseNET.coarsen","title":"<code>coarsen()</code>","text":"<p>Coarsens the graph</p> Source code in <code>src/minigraphs/miniaturize.py</code> <pre><code>def coarsen(self) -&gt; None:\n    '''Coarsens the graph\n    '''\n    self.G_coarse_ = self._G.to_directed()\n    n = self.G_coarse_.number_of_nodes()\n    n_edges = self.G_coarse_.number_of_edges()\n    n_reduced = int(self._alpha * n)\n\n    # Compute the eigenvalue and eigenvectors\n    self.lambda_, self.u_, self.v_ = CoarseNET.eigs(self.G_coarse_)\n\n    # Arrays of nodes and edges\n    self.nodes_coarse_ = np.arange(0,n,dtype=np.int32)\n    self.nodes_removed_ = []\n    edges = list(self.G_coarse_.edges)\n\n    # Calculate sorting indices according to score\n    score = self.__score()\n    idx = np.argsort(score)\n\n    contractions = 0\n    i = 0\n    while (contractions &lt; n_reduced) and (i &lt; n_edges):\n        # Retrieve edge according to sorting\n        edge = edges[idx[i]]\n\n        # Contract edges\n        contract = self.__contract(edge)\n\n        if contract:\n            contractions += 1\n\n        i += 1\n</code></pre>"},{"location":"reference/miniaturize/#minigraphs.miniaturize.CoarseNET.eigs","title":"<code>eigs(G)</code>  <code>staticmethod</code>","text":"<p>Computes the dominant eigenvalue and eigenvectors of the Adjacency matrix of a graph</p> Source code in <code>src/minigraphs/miniaturize.py</code> <pre><code>@staticmethod\ndef eigs(G):\n    '''Computes the dominant eigenvalue and eigenvectors of the Adjacency matrix of a graph\n    '''\n    # Adjacency Matrix\n    A = CoarseNET.adjacency(G)\n\n    # Compute the first eigenvalue and right eigenvector\n    lambda_, u_ = scipy.sparse.linalg.eigs(A,k=1)\n\n    # Compute the left eigenvector\n    _, v_= scipy.sparse.linalg.eigs(A.T,k=1)\n\n    return np.real(lambda_)[0], np.real(np.squeeze(u_)), np.real(np.squeeze(v_))\n</code></pre>"},{"location":"reference/simulation/","title":"Models Module","text":"Source code in <code>src/minigraphs/simulation/models.py</code> <pre><code>class Simulation:\n    def __init__(self,adjacency):\n        '''Initializes the simulation object on an adjacency matrix\n        '''\n        self._A = adjacency\n\n    @property\n    def trajectories_(self):\n        return self._trajectories_\n\n    @property\n    def trajectories_df_(self):\n        df = pd.DataFrame(self._trajectories_,columns=self.columns_)\n        return df\n\n    @property\n    def n_agents(self):\n        return self._A.shape[0]\n\n    @property\n    def adjacency(self):\n        return self._A.copy()\n\n    def run(self,model, n_steps: int):\n        # Check model formulation\n        n_trajectories, self.columns_ = model.spec(self._A)\n\n        # Initialize array of trajectories\n        self._trajectories_ = np.zeros((n_steps,n_trajectories))\n\n        # Process adjacency matrix\n        A = model.process(self._A)\n\n        # Set initial conditions and calculate associated trajectories\n        states_buffer = np.zeros((2,self.n_agents))\n        states_buffer[0,:] = model.start(A)\n        self._trajectories_[0] = model.trajectories(A,states_buffer[0,:])\n\n        # Run simulation\n        idx_old = 0\n        idx_new = 1\n        for i in range(1,n_steps):\n            # Update states\n            model.step(A,states_buffer[idx_old,:],states_buffer[idx_new,:])\n\n            # Calculate trajectories\n            self._trajectories_[i] = model.trajectories(A,states_buffer[idx_new,:])\n\n            # Update buffer index\n            idx_old = (idx_old + 1) % 2\n            idx_new = (idx_new + 1) % 2\n</code></pre> Source code in <code>src/minigraphs/simulation/models.py</code> <pre><code>class Sir:\n    def __init__(self,tau: float,gamma: float, n_infected: int = 1):\n        '''Initialilzes the sir simulation object\n        '''\n        #TODO: Implement check for bounds \n        self.tau = tau \n        self.gamma = gamma\n        self.n_infected = n_infected\n\n    def spec(self,A):\n        return 3, ('S','I','R')\n\n    def process(self,A):\n        return A\n\n    def start(self,A):\n        '''Initializes the state of each agent in the network\n        '''\n        n_agents = A.shape[0]\n\n        # Initialize agents as susceptible\n        states = np.zeros(n_agents,dtype=np.int32)\n\n        # Randomly select agents to be infected\n        idx = np.random.choice(n_agents,self.n_infected,replace=False)\n        states[idx] = 1\n\n        return states\n\n    def trajectories(self,A,states):\n        '''Calculates the simulation trajectories\n        '''\n        return np.sum(states[:,np.newaxis] == np.array([0,1,2]),axis=0) / states.shape[0]\n\n    def step(self,A,states_old,states_new):\n        '''Updates the states of the agents\n        '''\n        n_agents = A.shape[0]\n        self.beta = self.tau / n_agents\n        if self.beta &gt; 1.0:\n            raise ValueError(\"Beta must be smaller or equal than 1.0\")\n\n        for i in range(n_agents):\n            # Check current agent's state\n            state = states_old[i]\n\n            if state == 0:\n                # Obtain neighbors of the ith agent\n                row = A._getrow(i)\n\n                # Count number of infected neighbors\n                n_infected = int((states_old[row.indices] == 1).sum())\n\n                # Attempt infection according to the binomial distribution\n                if np.random.rand() &lt;= self.__p_infection(n_infected):\n                    states_new[i] = 1\n                else:\n                    states_new[i] = 0\n\n            elif state == 1:\n                # Recover infected agent with probability gamma\n                if np.random.rand() &lt;= self.gamma:\n                    states_new[i] = 2\n                else:\n                    states_new[i] = 1\n\n            else:\n                # Recovered individuals remain recovered\n                states_new[i] = 2\n\n    def __p_infection(self,n_contacts: int) -&gt; float:\n            p = n_contacts and (n_contacts * self.beta * ((1-self.beta)**(n_contacts-1)))\n            return p\n</code></pre> Source code in <code>src/minigraphs/simulation/models.py</code> <pre><code>class DeGroot:        \n    def process(self,A):\n        return normalize(A,axis=0,norm='l1')\n\n    def spec(self,A):\n        n_agents = A.shape[0]\n        return n_agents, np.arange(n_agents,dtype=np.int32)\n\n    def start(self,A):\n        '''Initializes the state of each agent\n        '''\n        n_agents = A.shape[0]\n        state = np.random.rand(n_agents)\n        return state \n\n    def trajectories(self,A,state):\n        '''Computes the trajectories of the agents\n        '''\n        return state\n\n    def step(self,A,states_old,states_new):\n        states_new[:] = A.dot(states_old)\n</code></pre>"},{"location":"reference/simulation/#minigraphs.simulation.models.Simulation.__init__","title":"<code>__init__(adjacency)</code>","text":"<p>Initializes the simulation object on an adjacency matrix</p> Source code in <code>src/minigraphs/simulation/models.py</code> <pre><code>def __init__(self,adjacency):\n    '''Initializes the simulation object on an adjacency matrix\n    '''\n    self._A = adjacency\n</code></pre>"},{"location":"reference/simulation/#minigraphs.simulation.models.Sir.__init__","title":"<code>__init__(tau, gamma, n_infected=1)</code>","text":"<p>Initialilzes the sir simulation object</p> Source code in <code>src/minigraphs/simulation/models.py</code> <pre><code>def __init__(self,tau: float,gamma: float, n_infected: int = 1):\n    '''Initialilzes the sir simulation object\n    '''\n    #TODO: Implement check for bounds \n    self.tau = tau \n    self.gamma = gamma\n    self.n_infected = n_infected\n</code></pre>"},{"location":"reference/simulation/#minigraphs.simulation.models.Sir.start","title":"<code>start(A)</code>","text":"<p>Initializes the state of each agent in the network</p> Source code in <code>src/minigraphs/simulation/models.py</code> <pre><code>def start(self,A):\n    '''Initializes the state of each agent in the network\n    '''\n    n_agents = A.shape[0]\n\n    # Initialize agents as susceptible\n    states = np.zeros(n_agents,dtype=np.int32)\n\n    # Randomly select agents to be infected\n    idx = np.random.choice(n_agents,self.n_infected,replace=False)\n    states[idx] = 1\n\n    return states\n</code></pre>"},{"location":"reference/simulation/#minigraphs.simulation.models.Sir.step","title":"<code>step(A, states_old, states_new)</code>","text":"<p>Updates the states of the agents</p> Source code in <code>src/minigraphs/simulation/models.py</code> <pre><code>def step(self,A,states_old,states_new):\n    '''Updates the states of the agents\n    '''\n    n_agents = A.shape[0]\n    self.beta = self.tau / n_agents\n    if self.beta &gt; 1.0:\n        raise ValueError(\"Beta must be smaller or equal than 1.0\")\n\n    for i in range(n_agents):\n        # Check current agent's state\n        state = states_old[i]\n\n        if state == 0:\n            # Obtain neighbors of the ith agent\n            row = A._getrow(i)\n\n            # Count number of infected neighbors\n            n_infected = int((states_old[row.indices] == 1).sum())\n\n            # Attempt infection according to the binomial distribution\n            if np.random.rand() &lt;= self.__p_infection(n_infected):\n                states_new[i] = 1\n            else:\n                states_new[i] = 0\n\n        elif state == 1:\n            # Recover infected agent with probability gamma\n            if np.random.rand() &lt;= self.gamma:\n                states_new[i] = 2\n            else:\n                states_new[i] = 1\n\n        else:\n            # Recovered individuals remain recovered\n            states_new[i] = 2\n</code></pre>"},{"location":"reference/simulation/#minigraphs.simulation.models.Sir.trajectories","title":"<code>trajectories(A, states)</code>","text":"<p>Calculates the simulation trajectories</p> Source code in <code>src/minigraphs/simulation/models.py</code> <pre><code>def trajectories(self,A,states):\n    '''Calculates the simulation trajectories\n    '''\n    return np.sum(states[:,np.newaxis] == np.array([0,1,2]),axis=0) / states.shape[0]\n</code></pre>"},{"location":"reference/simulation/#minigraphs.simulation.models.DeGroot.start","title":"<code>start(A)</code>","text":"<p>Initializes the state of each agent</p> Source code in <code>src/minigraphs/simulation/models.py</code> <pre><code>def start(self,A):\n    '''Initializes the state of each agent\n    '''\n    n_agents = A.shape[0]\n    state = np.random.rand(n_agents)\n    return state \n</code></pre>"},{"location":"reference/simulation/#minigraphs.simulation.models.DeGroot.trajectories","title":"<code>trajectories(A, state)</code>","text":"<p>Computes the trajectories of the agents</p> Source code in <code>src/minigraphs/simulation/models.py</code> <pre><code>def trajectories(self,A,state):\n    '''Computes the trajectories of the agents\n    '''\n    return state\n</code></pre>"}]}